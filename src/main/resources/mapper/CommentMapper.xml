<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.twilight.campus.mapper.CommentMapper">

    <!-- 根据内容ID查询评论列表 -->
    <select id="selectByContentId" resultType="com.twilight.campus.pojo.Comment">
        select
        id,
        content_id,
        user_id,
        parent_id,
        comment_text,
        status,
        create_time
        from comment
        where content_id = #{contentId}
        and status = 1
        order by create_time asc
    </select>

    <!-- 根据评论ID查询 -->
    <select id="selectById" resultType="com.twilight.campus.pojo.Comment">
        select
        id,
        content_id,
        user_id,
        parent_id,
        comment_text,
        status,
        create_time
        from comment
        where id = #{id}
        limit 1
    </select>

    <!-- 新增评论 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into comment
        (content_id, user_id, parent_id, comment_text, status, create_time)
        values
        (#{contentId}, #{userId}, #{parentId}, #{commentText}, #{status}, now())
    </insert>

    <!-- 修改评论状态 -->
    <update id="updateStatus">
        update comment
        set status = #{status}
        where id = #{id}
    </update>

</mapper>
