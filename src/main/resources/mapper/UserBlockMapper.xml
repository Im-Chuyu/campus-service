<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.twilight.campus.mapper.UserBlockMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user_block (blocker_id, blocked_user_id, create_time)
        values (#{blockerId}, #{blockedUserId}, now())
    </insert>

    <delete id="deleteByBlockerAndBlocked">
        delete from user_block
        where blocker_id = #{blockerId}
          and blocked_user_id = #{blockedUserId}
    </delete>

    <select id="countByBlockerAndBlocked" resultType="int">
        select count(*)
        from user_block
        where blocker_id = #{blockerId}
          and blocked_user_id = #{blockedUserId}
    </select>

    <select id="countByUserId" resultType="int">
        select count(*)
        from user_block
        where blocker_id = #{userId}
           or blocked_user_id = #{userId}
    </select>

</mapper>
