<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.twilight.campus.mapper.LikeRecordMapper">

    <select id="selectByUserIdAndContentId" resultType="com.twilight.campus.pojo.LikeRecord">
        select id, user_id, content_id, create_time
        from like_record
        where user_id = #{userId}
        and content_id = #{contentId}
        limit 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into like_record
        (user_id, content_id, create_time)
        values
        (#{userId}, #{contentId}, now())
    </insert>

    <delete id="deleteByUserIdAndContentId">
        delete from like_record
        where user_id = #{userId}
        and content_id = #{contentId}
    </delete>

    <select id="selectByUserId" resultType="com.twilight.campus.pojo.LikeRecord">
        select id, user_id, content_id, create_time
        from like_record
        where user_id = #{userId}
        order by create_time desc
    </select>

</mapper>
